# Copy to .env (never commit real secrets)
APP_ENV=development
VITE_API_BASE_URL=https://api.example.com

# Optional client-exposed keys (add only if used; keep empty placeholders)
VITE_SENTRY_DSN=
VITE_ANALYTICS_KEY=
VITE_FEATURE_FLAGS_KEY=
